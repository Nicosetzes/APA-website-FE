import MenuWrapper from "./../MenuWrapper";
import { StyledNavbar } from "./styled";

const Navbar = () => {
  return (
    <StyledNavbar>
      <MenuWrapper />
    </StyledNavbar>
  );
};

export default Navbar;
